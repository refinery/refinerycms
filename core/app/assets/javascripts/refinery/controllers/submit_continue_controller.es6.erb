window.application.register('refinery--admin--core--submit-continue', class extends Stimulus.Controller {
  submit(e) {
    e.preventDefault()

    // TODO: Refactor this function
    submit_and_continue(e)
  }
})