<% if params[list_id ||= "sortable_list"].present? and
      (continue_reordering ||= "true") == "true" -%>
  <script>
    $(document).ready(function(){
      list_reorder.enable_reordering();
    });
  </script>
<% end -%>
