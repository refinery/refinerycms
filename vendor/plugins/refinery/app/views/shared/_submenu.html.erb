<% page = @page.root %>
<ul id='submenu'<%= " class='empty'" if page.children.empty? %>>
<% 
unless page.children.empty?
	page.children.each do |child| %>
	<%= render :partial => "/shared/submenu_branch", :locals => {:page => child} %>
<% end
end %>
</ul>