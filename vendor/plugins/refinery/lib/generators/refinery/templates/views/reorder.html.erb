<h1>Reordering <%= $title_name.pluralize %></h1>
<div id='actions'>
	<h2>Instructions</h2>
	<p>
		<em>Click and drag</em> the <%= plural_name %> into the order you desire.
		The order is saved as you move the <%= plural_name %>. Click the "Done" button when you're done.
	</p>
	<h2>Ordering Status</h2>
	<div id='sort-status'>Order has not changed yet.</div>
</div>
<div id='records'>
	<div id='<%= plural_name %>_list'>
		<%%= render :partial => "list", :locals => { :<%= plural_name %> => @<%= plural_name %>} %>
	</div>
	<div class='form-actions'>
		<%%= button_to 'Done', admin_<%= plural_name %>_url, :method => :get %>
	</div>
</div>