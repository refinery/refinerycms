<% content_for :admin_sidebar do %>
  <%= render 'form' %>
<% end %>

<% content_for :admin_page_preview do %>
  <%= render template: "refinery/pages/#{@page.view_template.presence || (@page.link_url == '/' ? 'home' : 'show')}", locals: {editing: true} %>
<% end %>
