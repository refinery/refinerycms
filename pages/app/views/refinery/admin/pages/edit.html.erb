<% content_for :admin_sidebar do %>
  <%= render 'form' %>
<% end %>

<% content_for :admin_page_preview do %>
  <%
    render_options = {}
    render_options[:template] = "refinery/pages/#{@page.view_template.presence || (@page.link_url == '/' ? 'home' : 'show')}"
  %>
  <%= render render_options %>
<% end %>
