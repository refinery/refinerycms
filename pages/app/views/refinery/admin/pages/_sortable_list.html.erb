<ul id='sortable_list' class="list-unstyled tree" role="tablist">
  <%= render :partial => 'page', :collection => @root_pages.present? ? @root_pages : @pages.roots  %>
</ul>
<%= render '/refinery/admin/sortable_list', :continue_reordering => !!local_assigns[:continue_reordering] %>
