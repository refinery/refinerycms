<div id='records'>
  <%= will_paginate @users %>
  <ul>
    <%= render :partial => "user", :collection => @users %>
  </ul>
  <%= will_paginate @users %>
</div>
<div id='actions'>
  <ul>
    <li>
      <%= link_to t('.create_new_user'), new_admin_user_url, :class => "add_icon" %>
    </li>
  </ul>
</div>
